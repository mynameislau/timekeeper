{
  "name": "buyco-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon back/index.js && cd front && ng serve",
    "build-pwa": "cd front && ng build --prod && cd .. && http-server -p 8080 -c-1 -S -C localhost.crt -K localhost.key -P https://laurent-falda.ilucca-demo.net front/dist/time-manager"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.12.3",
    "json-server": "^0.16.1",
    "nodemon": "^2.0.3"
  }
}
